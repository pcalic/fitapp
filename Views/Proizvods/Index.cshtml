@model IEnumerable<FitnessApp.Models.DAL.Proizvod>

@{
    ViewBag.Title = "Index";
}

<h3>Kreiraj proizvod</h3>

<p>
    @Html.ActionLink("Dodaj", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cena)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Opis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vrsta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cena)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Opis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vrsta)
        </td>
        <td>
            @Html.ActionLink("Izmeni", "Edit", new { id=item.ProizvodId }) |
            @Html.ActionLink("Detalji", "Details", new { id=item.ProizvodId }) |
            @Html.ActionLink("Obriši", "Delete", new { id=item.ProizvodId })
        </td>
    </tr>
}

</table>
