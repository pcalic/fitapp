@model IEnumerable<FitnessApp.Models.DAL.Trener>

@{
    ViewBag.Title = "Index";
}

<h3>Trener</h3>

<p>
    @Html.ActionLink("Dodaj", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prezime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Godine)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prezime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Godine)
        </td>
        <td>
            @Html.ActionLink("Izmeni", "Edit", new { id=item.TrenerId }) |
            @Html.ActionLink("Detalji", "Details", new { id=item.TrenerId }) |
            @Html.ActionLink("Obriši", "Delete", new { id=item.TrenerId })
        </td>
    </tr>
}

</table>
